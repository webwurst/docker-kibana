elasticsearch:
  # image: webwurst/elasticsearch
  image: dockerelasticsearch_elasticsearch
  ports:
    - "9200"

rsyslog:
  image: webwurst/rsyslog
  ports:
    - "514:514"
  links:
    - elasticsearch

kibana:
  build: .
  ports:
    - "5601:5601"
  links:
    - elasticsearch
